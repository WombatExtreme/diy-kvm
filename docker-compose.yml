version: '3.8'
services:
  kvm:
    build: .
    privileged: true
    network_mode: host
    devices:
      - "/dev/input:/dev/input"
      - "/dev/ttyUSB0:/dev/ttyUSB0"
    volumes:
      - ".:/app"
      - "/var/run/docker.sock:/var/run/docker.sock"
    restart: always